{{#if export}}export {{/if}}function {{name}}({{#each parameters}}{{name}}: {{dataType}}{{#unless @last}}, {{/unless}}{{/each}}){{#if returnDataType}}: {{returnDataType}}{{/if}} {
    {{#with implementation}}
    {{> (lookup . "templateUri") context method=..}}
    {{/with}}
}
