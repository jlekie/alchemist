labels:
  app.cohesion.io/name: alchemist
  app.cohesion.io/platform: nodejs
  app.cohesion.io/platform/language: typescript
tasks:
  - name: lint
    actions:
      - type: node.script
        script: lint
  - name: build
    actions:
      - type: node.script
        script: build
  - name: watch
    actions:
      - type: node.script
        script: watch
  - name: run
    actions:
      - type: node.script
        script: start
  - name: transmute
    actions:
      - type: node.script
        script: start transmute
