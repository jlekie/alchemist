{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Server",
            "program": "${workspaceFolder}/dist/run.js",
            "console": "integratedTerminal",
            "args": [ "start" ],
            "preLaunchTask": "build"
        }
    ]
}